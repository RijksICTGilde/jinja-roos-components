{#
DEPRECATED: This file is kept for backward compatibility only.
Color definitions are now loaded from definitions.json and injected as
the global variable 'rvo_color_names' in the Jinja environment.

To use colors in templates, check against rvo_color_names instead:
  {% set is_named_color = color_lower in rvo_color_names %}
  {% set resolved_color = 'var(--rvo-color-' + color_lower + ')' if is_named_color else color %}

This file may be removed in a future version.
#}

{# Legacy rvo_colors mapping - kept for backward compatibility #}
{% set rvo_colors = {} %}
{% for color_name in rvo_color_names %}
  {% set _ = rvo_colors.update({color_name: 'var(--rvo-color-' + color_name + ')'}) %}
{% endfor %}